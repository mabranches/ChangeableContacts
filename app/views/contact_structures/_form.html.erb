<%= form_for(contact_structure) do |f| %>
  <% if contact_structure.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(contact_structure.errors.count, "error") %> prohibited this contact_structure from being saved:</h2>

      <ul>
      <% contact_structure.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.text_field :user_id %>
  </div>

  <div class="field">
    <%= f.label :data_type_id %>
    <%= f.text_field :data_type_id %>
  </div>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :extra %>
    <%= f.text_field :extra %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
